# SentinelStack TODO

Stack features from the former backlog are implemented in the repo (dashboard refresh controls, Port Guard schedule DB persistence + last sweep time + per-scan duration, CSV export, `scripts/validate_stack.ps1`).

**Optional follow-ups**

- Manually run `scripts/validate_stack.ps1` after generating traffic; tune rule thresholds in `.env` to match your lab (Milestone 6 style validation).
- **Commit / push** when ready — not automated here.
